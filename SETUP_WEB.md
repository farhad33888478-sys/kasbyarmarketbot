# راهنمای راه‌اندازی وب کسب‌یار مارکت

## قدم اول: نصب پیش‌نیازها
یک‌بار این دستور را در پوشه پروژه اجرا کن:

```
pip install -r requirements.txt
```

## قدم دوم: تنظیم فایل .env
فایل `.env` از قبل با اطلاعات فعلی ربات پر شده. فقط دو مورد را حتماً عوض کن:

- `WEB_SECRET_KEY` → یک رشته‌ی تصادفی طولانی بگذار
- `ADMIN_WEB_USERNAME` و `ADMIN_WEB_PASSWORD` → نام کاربری و رمز ورود به پنل مدیریت وب

⚠️ فایل `.env` را هرگز جایی عمومی (مثل گیت‌هاب) آپلود نکن. در `.gitignore` قرار دارد.

## قدم سوم: اجرا

**فقط وب:**
دوبار کلیک روی `start_web.bat`

**ربات + وب با هم:**
دوبار کلیک روی `start_all.bat`

سایت روی این آدرس بالا می‌آید:
```
http://127.0.0.1:8000
```

پنل مدیریت:
```
http://127.0.0.1:8000/admin/login
```

## نکات مهم

- وب و ربات کاملاً مستقل از هم هستند؛ خاموش/روشن‌کردن یکی، دیگری را قطع نمی‌کند.
- هر دو از همان دیتابیس `kasbyarmarket.db` استفاده می‌کنند، پس کسب‌وکارهای ثبت‌شده در ربات و وب در یک‌جا مدیریت می‌شوند.
- اگر کاربری در ربات با شماره موبایلش ثبت‌نام کرده باشد و بعداً با همان شماره در وب هم ثبت‌نام کند، دو حساب به‌صورت خودکار به هم متصل می‌شوند.
- برای این‌که سایت با یک آدرس اینترنتی (مثل kasbyarmarket.ir) در دسترس همه باشد، باید روی یک سرور (نه کامپیوتر شخصی) میزبانی شود و یک دامنه ثبت گردد — این قدم بعدی پیشنهادی است.
